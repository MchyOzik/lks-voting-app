<<<<<<< HEAD
name: Deploy vote app
=======
name: Deploy to Amazon ECS
>>>>>>> 7a908985d5b2c35184fdaaea154c66164128dade

on:
  push:
    branches: [ "main" ]

env:
  AWS_REGION: us-east-1
<<<<<<< HEAD
  ECR_REPOSITORY: lks-voting-image
  ECS_SERVICE: lks-vote-service
  ECS_CLUSTER: lks-voting-clusters
  ECS_TASK_DEFINITION: lks-td-vote
  ENV: "dev"
=======
  ECS_CLUSTER: lks-voting-clusters
  ECS_SERVICE: lks-vote-service
  ECS_TASK_DEFINITION: lks-td-vote
  ACCOUNT_ID: 210748260064
  ECR_REPOSITORY: lks-voting-image
>>>>>>> 7a908985d5b2c35184fdaaea154c66164128dade

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    environment: production

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
<<<<<<< HEAD
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
        aws-region: ${{ env.AWS_REGION }}

    - name: Login to Amazon ECR
      run: |
        aws ecr get-login-password --region ${{ env.AWS_REGION }} | docker login --username AWS --password-stdin ${{ env.ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com
        
        docker build -t ${{ env.ECR_REPOSITORY }} ./vote
        docker tag ${{ env.ECR_REPOSITORY }}:vote-latest ${{ env.ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/${{ env.ECR_REPOSITORY }}:-vote${{ github.sha }}
        docker push ${{ env.ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/${{ env.ECR_REPOSITORY }}:-vote${{ github.sha }}

    - name: Update image in taskdef.json
      run: |
        sed -i "s|IMAGE_URI|${{ env.ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/${{ env.ECR_REPOSITORY }}:-vote${{ github.sha }}|g" vote/task-definition.json
=======
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_KEY }}
        aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
        aws-region: ${{ env.AWS_REGION }}
    
    - name: Build and push image
      run: |
        aws ecr get-login-password --region ${{ env.AWS_REGION }} | docker login --username AWS --password-stdin ${{ env.ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com
        
        docker build -t ${{ env.ECR_REPOSITORY }}:vote-latest ./vote
        docker tag ${{ env.ECR_REPOSITORY }}:vote-latest ${{ env.ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/${{ env.ECR_REPOSITORY }}:vote-${{ github.sha }}
        docker push ${{ env.ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/${{ env.ECR_REPOSITORY }}:vote-${{ github.sha }}

    - name: Update image in taskdef.json
      run: |

        sed -i "s|IMAGE_URI|${{ env.ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/${{ env.ECR_REPOSITORY }}:vote-${{ github.sha }}|g" vote/task-definition.json

    - name: my-step
      run: echo "Hello World!"
>>>>>>> 7a908985d5b2c35184fdaaea154c66164128dade

    - name: Register task definition
      run: |
        aws ecs register-task-definition \
          --cli-input-json file://vote/task-definition.json

<<<<<<< HEAD
    - name: Pull image
      id: pull-image
      env:
        ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        WORKER_IMAGE_TAG: worker-${{ env.ENV }}-latest
        # Build docker images for vote module and push it to ECR so that it can be deployed to EKS
      run: |
          docker pull $ECR_REGISTRY/$ECR_REPOSITORY:$WORKER_IMAGE_TAG     

=======
>>>>>>> 7a908985d5b2c35184fdaaea154c66164128dade
    - name: Update service
      run: |
        aws ecs update-service \
          --cluster ${{ env.ECS_CLUSTER }} \
          --service ${{ env.ECS_SERVICE }} \
<<<<<<< HEAD
          --task-definition ${{ env.ECS_TASK_DEFINITION }} \
          --force-new-deployment

    - name: my-step
        run: echo "Hello World!"

    - name: Publish Message To SNS Subscribers
      env:
        TOPIC_ARN: ${{ secrets.AWS_SNS_ARN }}
      run: aws sns publish --topic-arn $TOPIC_ARN --subject "Hello, from Indonesia" --message "New Version Is Released"
    
=======
          --task-definition lks-td-vote \
          --force-new-deployment

    - name: Publish Message To SNS Subscribers
      env:
        TOPIC_ARN: ${{ secrets.AWS_SNS_ARN }}
      run: aws sns publish --topic-arn $TOPIC_ARN --subject "Hello from Jawa Timur!" --message "New version The Voting App from Jawa Timur is Released !!"
>>>>>>> 7a908985d5b2c35184fdaaea154c66164128dade
